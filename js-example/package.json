{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp browserify > output/index.js",
    "install": "bower install"
  },
  "dependencies": {
    "bip39": "^2.5",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "pulp": "^11.0.0",
    "purescript": "^0.11.1",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.4"
  }
}
