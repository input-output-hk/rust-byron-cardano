[package]
name = "protocol"
version = "0.1.0"
authors = ["vincent.hanquez@iohk.io"]
license = "MIT"
description = "protocol"
keywords = [ "Cardano", "Protocol" ]

[build-dependencies]

[dependencies]
raw_cbor = { path = "../cbor" }
cardano = { path = "../cardano" }
log = "0.4"
